akeneo:
  build: .
  environment:
    LINK_ALIAS: mysql
  links:
   - mysql
  ports:
   - "80:80"
mysql:
  image: mysql:5.6
  environment:
    MYSQL_ROOT_PASSWORD: root
    MYSQL_USER: akeneo_pim
    MYSQL_PASSWORD: akeneo_pim
    MYSQL_DATABASE: akeneo_pim
